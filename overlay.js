document.addEventListener('DOMContentLoaded', () => { const alerts = document.querySelectorAll('.alert, .alert2, .alert3'); alerts.forEach(alert => { alert.style.animation = 'fadeIn 1s ease-in'; setTimeout(() => { alert.style.display = 'none'; }, 5000); }); const chatBox = document.querySelector('.chat-box'); const messages = ['Welcome to the stream\!', 'Keep it chill\!', 'Thanks for watching\!']; let i = 0; setInterval(() => { chatBox.textContent = messages[i]; i = (i + 1) % messages.length; }, 3000); const goalBar = document.querySelector('.goal-bar'); goalBar.textContent = 'Subscriber Goal: 10/100'; });
